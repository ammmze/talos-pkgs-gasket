diff --git kernel/build/pkg.yaml kernel/build/pkg.yaml
index a799a48..8870ba1 100644
--- kernel/build/pkg.yaml
+++ kernel/build/pkg.yaml
@@ -16,6 +16,12 @@ steps:
       - |
         cd /src
         python3 /toolchain/kconfig-hardened-check/bin/kconfig-hardened-check -c .config -m json | python3 /pkg/scripts/filter-hardened-check.py
+      - |
+        set -e
+        grep 'gasket' /src/drivers/Kconfig
+        ls -la /src/drivers/staging/gasket
+        echo 'debugging'
+        exit 1
       - |
         cd /src
 
diff --git kernel/prepare/pkg.yaml kernel/prepare/pkg.yaml
index 37b01e3..2bd00c4 100644
--- kernel/prepare/pkg.yaml
+++ kernel/prepare/pkg.yaml
@@ -40,6 +40,11 @@ steps:
         done
 
         make mrproper
+      - |
+        set -e
+        patch -p0 < /pkg/patches/gasket.patch
+        grep 'gasket' drivers/Kconfig
+        ls -la drivers/staging/gasket
       - |
         cd /toolchain && git clone https://github.com/a13xp0p0v/kconfig-hardened-check.git
       - |
